def suggest(target, total_shoot_time, shoot_data):
    percentage = 0
    pivot_foot_bias = "right"
    hit_pos = "up"

    return {"percentage" : percentage, "pivot_foot_bias" : pivot_foot_bias, "hit_pos" : hit_pos}